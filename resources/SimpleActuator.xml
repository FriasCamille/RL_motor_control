<mujoco model="pendulum_torque">
    <compiler angle="radian"/>
    <option gravity="0 0 -9.81" timestep="0.001"/>

    <worldbody>
        <!-- Soporte fijo -->
        <body name="base" pos="0 0 0">
            <!-- Péndulo -->
            <body name="pendulum" pos="0 0 0">
                <joint 
                    name="hinge"
                    type="hinge"
                    axis="0 1 0"
                    limited="false"
                />
                <geom 
                    type="capsule"
                    fromto="0 0 0 0 0 -1"
                    size="0.05"
                    mass="1.0"
                />
            </body>
        </body>
    </worldbody>

    <actuator>
        <!-- Torque directo -->
        <motor 
            name="torque_motor"
            joint="hinge"
            gear="1"
            ctrlrange="-10 10"
        />
    </actuator>
</mujoco>
